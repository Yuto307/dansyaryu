<%= link_to favorites_path(post_id: post.id),
            id: "js-favorite-button-for-post-#{post.id}",
            method: :post,
            remote: true do %>
  <%= icon 'far', 'star' %>
<% end %>
