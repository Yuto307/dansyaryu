<%= link_to best_answer_path(current_user.best_answers.find_by(comment_id: comment.id)),
            id: "js-favorite-button-for-comment-#{comment.id}",
            method: :delete,
            remote: true do %>
  <%= icon 'fas', 'star' %>
<% end %>